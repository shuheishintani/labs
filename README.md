# labs

このリポジトリでは、学習・検証・プロトタイピングを目的とした小規模なプロジェクトを管理しています。

各プロジェクトは `projects/` 配下にディレクトリ単位で配置します。

## 使い方

- 各プロジェクトはディレクトリ単位で完結しています（例: `projects/rate-limiter/`）。
- まずは対象ディレクトリに移動して、その中の `README.md` を参照してください。

## プロジェクト一覧

- `projects/rate-limiter/`: レートリミッターの実装
